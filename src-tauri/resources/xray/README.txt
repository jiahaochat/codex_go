Xray.exe is not committed to this repository.
Run scripts/prepare-windows-assets.ps1 before a Windows package build. The script
downloads the pinned upstream archive and verifies both archive and executable
SHA-256 digests.
